{% set version = "2.5.6" %}

package:
  name: pdal-split
  version: {{ version }}

source:
  git_url: https://github.com/PDAL/PDAL.git
  git_rev: master

build:
  number: 3
  run_exports:
    - {{ pin_subpackage('libpdal', max_pin='x.x') }}

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - cmake
    - make
    - pkg-config

  host:
    - libgdal
    - geotiff
    - curl
    - zlib
    - zstd
    - libxml2
    - openssl

  run:
    - libgdal
    - geotiff
    - curl
    - zstd
    - libxml2
    - openssl

outputs:
  - name: libpdal
    script: install_lib.sh  # [unix]
    script: install_lib.bat  # [win]
    build:
      run_exports:
        # no idea, going with minor pin
        - {{ pin_subpackage('libpdal', max_pin='x.x') }}
    requirements:
      build:
        - {{ compiler('cxx') }}
        - {{ compiler('c') }}
        - cmake
        - make
        - pkg-config
      host:
        - {{ pin_compatible('libgdal', max_pin='x.x') }}
        - geotiff
        - libcurl
        - zlib
        - zstd
        - libxml2
        - openssl
      run:
        - {{ pin_compatible('libgdal', max_pin='x.x') }}
        - geotiff
        - libcurl
        - proj
        - zlib
        - zstd
        - libxml2
        - openssl
        - xerces-c
      run_constrained:   # [osx and x86_64]
        - __osx >={{ MACOSX_DEPLOYMENT_TARGET|default("10.9") }}  # [osx and x86_64]
    test:
      files:
        - test_libpdal.sh  # [unix]
      commands:
        - sh test_libpdal.sh  # [unix]
    about:
      summary: Point Data Abstraction Library (PDAL)
      license: BSD-3-Clause
      license_file: LICENSE.txt


  - name: pdal-trajectory
    script: build_trajectory.sh  # [unix]
    script: build_trajectory.bat  # [win]
    requirements:
      build:
        - {{ compiler('cxx') }}
        - {{ compiler('c') }}
        - cmake
        - make
      host:
        - {{ pin_subpackage('libpdal', exact=True) }}
        - {{ pin_compatible('libgdal', max_pin='x.x') }}
        - ceres-solver
        - glog
        - gflags
        - mkl  # [win]
        - blas  # [win]
      run:
        - {{ pin_subpackage('libpdal', exact=True) }}
        - {{ pin_compatible('libgdal', max_pin='x.x') }}
        - ceres-solver
        - mkl  # [win]
        - blas  # [win]
        - glog
        - gflags
    test:
      files:
        - test_trajectory.sh  # [unix]
      commands:
        - sh test_trajectory.sh  # [unix]
    about:
      summary: Point Data Abstraction Library (PDAL)
      license: BSD-3-Clause
      license_file: LICENSE.txt

  - name: pdal-hdf
    script: build_hdf.sh  # [unix]
    script: build_hdf.bat  # [win]
    requirements:
      build:
        - {{ compiler('cxx') }}
        - {{ compiler('c') }}
        - cmake
        - make
      host:
        - hdf5
        - {{ pin_subpackage('libpdal', exact=True) }}
      run:
        - hdf5
        - {{ pin_subpackage('libpdal', exact=True) }}
    test:
      files:
        - test_hdf.sh  # [unix]
      commands:
        - sh test_hdf.sh  # [unix]
    about:
      summary: Point Data Abstraction Library (PDAL)
      license: BSD-3-Clause
      license_file: LICENSE.txt


  - name: pdal-draco
    script: build_draco.sh  # [unix]
    script: build_draco.bat  # [win]
    requirements:
      build:
        - {{ compiler('cxx') }}
        - {{ compiler('c') }}
        - cmake
        - make
      host:
        - draco
        - {{ pin_subpackage('libpdal', exact=True) }}
      run:
        - draco
        - {{ pin_subpackage('libpdal', exact=True) }}
    test:
      files:
        - test_draco.sh  # [unix]
      commands:
        - sh test_draco.sh  # [unix]
    about:
      summary: Point Data Abstraction Library (PDAL)
      license: BSD-3-Clause
      license_file: LICENSE.txt

  - name: pdal-tiledb
    script: build_tiledb.sh  # [unix]
    script: build_tiledb.bat  # [win]
    requirements:
      build:
        - {{ compiler('cxx') }}
        - {{ compiler('c') }}
        - cmake
        - make
      host:
        - tiledb
        - {{ pin_compatible('libgdal', max_pin='x.x') }}
        - {{ pin_subpackage('libpdal', exact=True) }}
      run:
        - tiledb
        - {{ pin_compatible('libgdal', max_pin='x.x') }}
        - {{ pin_subpackage('libpdal', exact=True) }}
    test:
      files:
        - test_tiledb.sh  # [unix]
      commands:
        - sh test_tiledb.sh  # [unix]
    about:
      summary: Point Data Abstraction Library (PDAL)
      license: BSD-3-Clause
      license_file: LICENSE.txt

  - name: pdal-pgpointcloud
    script: build_pgpointcloud.sh  # [unix]
    script: build_pgpointcloud.bat  # [win]
    requirements:
      build:
        - {{ compiler('cxx') }}
        - {{ compiler('c') }}
        - cmake
        - make
      host:
        - libpq
        - libxml2
        - {{ pin_subpackage('libpdal', exact=True) }}
      run:
        - libpq
        - libxml2
        - {{ pin_subpackage('libpdal', exact=True) }}
    test:
      files:
        - test_pgpointcloud.sh  # [unix]
      commands:
        - sh test_pgpointcloud.sh  # [unix]
    about:
      summary: Point Data Abstraction Library (PDAL)
      license: BSD-3-Clause
      license_file: LICENSE.txt

  - name: pdal-arrow
    script: build_arrow.sh  # [unix]
    script: build_arrow.bat  # [win]
    requirements:
      build:
        - {{ compiler('cxx') }}
        - {{ compiler('c') }}
        - cmake
        - make
      host:
        - libarrow
        - {{ pin_subpackage('libpdal', exact=True) }}
      run:
        - libarrow
        - {{ pin_subpackage('libpdal', exact=True) }}
    test:
      files:
        - test_arrow.sh  # [unix]
      commands:
        - sh test_arrow.sh  # [unix]
    about:
      summary: Point Data Abstraction Library (PDAL)
      license: BSD-3-Clause
      license_file: LICENSE.txt

  - name: pdal-nitf
    script: build_nitf.sh  # [unix]
    script: build_nitf.bat  # [win]
    requirements:
      build:
        - {{ compiler('cxx') }}
        - {{ compiler('c') }}
        - cmake
        - make
      host:
        - nitro
        - libcurl
        - {{ pin_subpackage('libpdal', exact=True) }}
      run:
        - nitro
        - libcurl
        - {{ pin_subpackage('libpdal', exact=True) }}
    test:
      files:
        - test_nitf.sh  # [unix]
      commands:
        - sh test_nitf.sh  # [unix]
    about:
      summary: Point Data Abstraction Library (PDAL)
      license: BSD-3-Clause
      license_file: LICENSE.txt


about:
  home: https://www.pdal.io
  license: BSD-3-Clause
  license_file: LICENSE.txt
  summary: Point Data Abstraction Library (PDAL)
  description: Point cloud data processing.
  doc_url: https://pdal.io
  dev_url: https://github.com/pdal/pdal

extra:
  recipe-maintainers:
    - chambbj
    - hobu
